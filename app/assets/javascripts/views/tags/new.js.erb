var _init_tag_splat;
var tag_splat_button = $("#tag_splat");

_init_tag_splat = function init_tag_splat() {
  if (tag_splat_button.length) {
    // TODO: We need to bew able to accommodate multiple tag_splats on a given, page, each with a
    // different object type (i.e. anything which includes Taggable.
    // Gotta do something here...
  }

};
