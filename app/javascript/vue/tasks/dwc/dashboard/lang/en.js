//  Dwc dashboard
//  language: en

export default {
  section: {
    freshnessIndex: 'A graph of when, if ever, an indexed record has been created both overal (Index), and per type of indexable object (e.g. CollectionObject).',
    freshnessVersions: 'A graph indicating the version of the index used for created indexed record per class of indexable object (e.g. CollectionObject). When the indexing changes how fields in the DwC are changed, the a timestamp, i.e. version, indicating when this change happens is added. Bars represent time ranges, the first version starts from 2000 to the earliest date, the last version is from the last date to the present time.'
  }
}
