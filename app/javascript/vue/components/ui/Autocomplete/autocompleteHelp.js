// Optional help text shown (via the global `data-help` tooltip system) on
// Autocomplete instances whose `url` targets one of these resources. Keyed
// by the resource segment of the url, e.g. '/geographic_areas/autocomplete'
// -> 'geographic_areas'.
export const AUTOCOMPLETE_HELP = {
  geographic_areas:
    "Type two words separated by a space to match a place and its parent, e.g. 'wa te' matches 'Washington Co., Texas'."
}
