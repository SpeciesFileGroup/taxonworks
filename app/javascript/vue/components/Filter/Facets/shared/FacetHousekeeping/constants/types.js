import { COLLECTING_EVENT, COLLECTION_OBJECT } from '@/constants'

export const TYPE_MESSAGE = {
  [COLLECTING_EVENT]: 'All check: Data attributes, Georeferences, Notes, Roles.',
  [COLLECTION_OBJECT]: 'All check: Notes, Data attributes, Roles, Collecting event (and its Roles, Data attributes, Notes, Georeferences), Taxon determinations'
}
