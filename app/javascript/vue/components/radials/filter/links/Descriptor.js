import {
  FILTER_CITATION,
  FILTER_OBSERVATION,
  FILTER_OTU,
  FILTER_SOURCE,
} from '../constants/filterLinks'

export const Descriptor = [
  FILTER_CITATION,
  FILTER_OBSERVATION,
  FILTER_OTU,
  FILTER_SOURCE
]
