import {
  FILTER_OBSERVATION,
  FILTER_OTU,
  FILTER_SOURCE,
} from '../constants/filterLinks'

export const Desriptor = {
  all: [
    FILTER_OBSERVATION,
    FILTER_OTU,
    FILTER_SOURCE,
  ],
  ids: [ 
    FILTER_OBSERVATION,
    FILTER_OTU,
  ]
}