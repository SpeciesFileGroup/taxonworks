import {
    FILTER_CITATION,
    FILTER_IMAGE,
    FILTER_OTU,
    FILTER_SOURCE,
} from '../constants/filterLinks'

export const Content = [
    FILTER_CITATION,
    FILTER_IMAGE,
    FILTER_OTU,
    FILTER_SOURCE,
]
