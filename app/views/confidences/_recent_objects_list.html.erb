<ul>
  <% @recent_objects.each do |r| -%>
      <li> <%= link_to(object_tag(r.confidence_level) + ' on ' + object_tag(r.confidence_object) + ' (a ' + kind(r.confidence_object), r.confidence_object.metamorphosize) + ')' -%> <%= object_time_since_update_tag(r) -%> ago</li>
  <% end %>
</ul>

<%= content_tag(:em, "No recently updated or created #{@data_model.name.tableize.humanize}.") if @recent_objects.empty? -%>

