<%= form_for(@document, multipart: true) do |f| %>
    <%= render 'shared/errors', object: @document %>

    <div class="field">
      <%= f.label :document_file %><br>
      <div> <%= document_tag(@document) -%> </div>
      <%= f.file_field :document_file, class: :required_for_submit %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
