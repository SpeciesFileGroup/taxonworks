<%= form_for(@otu_page_layout) do |f| %>
    <%= render 'shared/errors', object: @otu_page_layout %>

    <div class="field">
      <%= f.label :name %>
      <br>
      <%= f.text_field :name %>
    </div>

    <%= render partial: '/topics/topic_picker', locals: {object: @otu_page_layout, f: f} %>

    <div class="actions">
      <%= f.submit %>
    </div>
    
    <%#= search_form_for_model(Topic) %>

    <!----
    <%# OtuPageLayoutSection::DynamicSection.descendants.each do |d| -%>
      <%#= [d.title, d.description].join(' - ') -%> <%#= check_box_tag(:section, value: d.name, otu_page_layout_id: @otu_page_layout.to_param, remote: true, url: '/') -%>
    <%# end %> 
    ---->

<% end %>
