$(document).bind('ajax:complete', function () {
  $('#unfavorite_link_<%= params[:kind] %>-<%= params[:name] %>').replaceWith("<%= escape_javascript(favorite_page_link(params[:kind], params[:name])) %>");
});
