<span class="<%= taxon_name.rank_class ? taxon_name.rank_class.nomenclatural_code : nil -%> "> 
  <%= taxon_name_tag(taxon_name).gsub(/(#{term})/i, content_tag(:mark, '\1')).html_safe -%>
  <span class="subtle">
  (<%= taxon_name.rank ? taxon_name.rank : 'Combination' -%><%= ( ', parent ' + taxon_name_tag(taxon_name.parent) ).html_safe if taxon_name.parent  -%>)
</span>
</span>
