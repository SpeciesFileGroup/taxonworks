<%= form_for(@citation_topic) do |f| %>
  <% if @citation_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@citation_topic.errors.count, "error") %> prohibited this citation_topic from being saved:</h2>

      <ul>
      <% @citation_topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
