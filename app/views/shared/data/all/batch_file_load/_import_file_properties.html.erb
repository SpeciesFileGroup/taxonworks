<h3> Import file properties </h3>
<ul>
  <li>Successfully
    processed: <%= content_tag(:span, (@result.processed ? 'yes' : 'no'), class: (@result.processed ? 'passed' : 'warning')) -%></li>
  <li>Total rows (total/with data/empty):
    <%= @result.total_lines ? @result.total_lines : '?' -%>
    /<%= @result.total_data_lines ? @result.total_data_lines : '?' -%>
    /<%= @result.total_lines && @result.total_data_lines ? (@result.total_lines - @result.total_data_lines) : '?' -%>
  </li>

  <li> This file
    is <%= content_tag(:span, @result.warn_level_ok? ? 'OK' : 'NOT OK', class: (@result.warn_level_ok? ? 'passed' : 'warning')) -%>
    for import level "warn".
  </li>
  <li> This file
    is <%= content_tag(:span, @result.line_strict_level_ok? ? 'OK' : 'NOT OK', class: (@result.line_strict_level_ok? ? 'passed' : 'warning')) -%>
    for import level "line strict".
  </li>
  <li> This file
    is <%= content_tag(:span, @result.strict_level_ok? ? 'OK' : 'NOT OK', class: (@result.strict_level_ok? ? 'passed' : 'warning')) -%>
    for import level "strict".
  </li>

  <%= @result.file_errors.collect { |e| content_tag(:li, content_tag(:span, e, class: 'warning')) }.join.html_safe -%>
  <%= @result.errors.collect { |e| content_tag(:li, content_tag(:span, e, class: 'warning')) }.join.html_safe -%>
</ul>
