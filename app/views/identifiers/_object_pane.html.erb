<div class="object_pane">
  <h3> Identifiers </h3>
  <ul>
    <% object.identifiers.each do |note| -%>
      <li> <%= identifier.cache -%> </li>
    <% end %>
  </ul>

  <%= content_tag(:em, 'No identifiers.') if !object.identifiers.any? -%>
</div>
