<div id="project-new">
  <div class="container-xl mx-auto">
    <header class="form-page-header">
      <span class="form-page-icon">
        <%= icon('square-pen', size: 28) %>
      </span>
      <div class="form-page-titles">
        <h1>Edit project</h1>
        <p class="form-page-subtitle"><%= @project.name %></p>
      </div>
    </header>

    <%= render 'form' %>
  </div>
</div>
