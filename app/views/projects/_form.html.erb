<%= form_for(@project) do |f| %>
    <%= render 'shared/errors', object: @project %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
