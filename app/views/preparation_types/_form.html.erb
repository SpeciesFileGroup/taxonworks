<%= form_for(@preparation_type) do |f| %>
    <% if @preparation_type.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@preparation_type.errors.count, "error") %> prohibited this preparation type from being saved:</h2>

          <ul>
            <% @preparation_type.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %>
      <br>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :definition %>
      <br>
      <%= f.text_area :definition %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
