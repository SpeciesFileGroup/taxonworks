<br>

<%= radial_navigation_tag(@lead) %><br>
<%= link_to('Table-format version',
    print_key_table_task_path(lead_id: @lead.id, lead_items: params[:lead_items])
  )
%>
<%= print_key(@lead, params[:lead_items] || false) %>

