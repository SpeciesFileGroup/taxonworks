<%= form_tag create_verbatim_reference_task_path do %>

  <h2>Create a source (reference) from a verbatim citation</h2>
  <p> <i> The record will be resolved against CrossRef, and you will be given an option
    to use the BibTeX record (if found) or store it as a verbatim record. </i> </p>
    <div>
      <div class="field">
        <%= label_tag :paste_full_citation %>
        <%= text_area_tag :citation, nil ,  rows: 4, cols: 50 %>
      </div>
    </div>
    <div class="actions">
      <%= submit_tag :preview %>
    </div>
<% end %>
