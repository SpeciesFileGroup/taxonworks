<div id="_recent_gr_form" name="_recent_gr_form" hidden>
  <%= form_tag(recent_georeferences_url, {method: 'get', id: 'recent_gr_count', remote: true}) do %>
      <table>
        <tr>
          <td class="field"><span>
              <label for="how_many">Return</label>
              <input id="how_many" size="5" name="how_many"/> most recent
              </span>
          </td>
        </tr>
      </table>
      <%= submit_tag('Find') %>
  <% end %>
</div>
