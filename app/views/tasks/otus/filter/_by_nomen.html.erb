<%= form_tag(set_nomen_for_otu_filter_path, id: 'set_nomen_form', remote: true) do %>
  Descendants (if available): <%= check_box_tag('descendants') %><br>
  <%= render('/workbench/elements/forms/ajax_select',
             controller: '/taxon_names',
             object:     '',
             method:     'taxon_name_id',
             tag_id:     'taxon_name_id_for_by_nomen',
             size:       20,
             display:    '')
  %>
  <%= rank_for_otu_select_tag(taxon_name: TaxonName.new, options: {disabled: true}) %><br>
  <%= submit_tag('Set Nomenclature', id: 'set_nomen') %>
<% end %>
