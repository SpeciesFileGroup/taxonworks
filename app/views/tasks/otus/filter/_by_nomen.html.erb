<%= form_tag(set_nomen_for_otu_filter_path, id: 'set_nomen_form', remote: true) do %>
  Descendants (if available): <%= check_box(nil, 'descendants', {id: :descendant_toggle}, '_on_', '_off_') %><br>
  <%= render('/workbench/elements/forms/ajax_select',
             controller: '/taxon_names',
             object:     '',
             method:     'nomen_id',
             tag_id:     'nomen_id_for_by_nomen',
             size:       20,
             display:    '')
  %>
  <%= otu_taxon_name_rank_select_tag(taxon_name: TaxonName.new, options: {disabled: true}) %><br>
  <%= submit_tag('Set Nomenclature', id: 'set_nomen') %>
<% end %>
