<%= content_for :report do -%>
    <% if @person.roles.any? %>
        <ul class="no_bullets">
          <%= content_tag(:li, link_to('Roles', roles_person_path(@person))) %>
        </ul>
    <% end %>
<% end %>

<%= render(partial: 'shared/data/project/show', locals: {object: @person}) -%>

