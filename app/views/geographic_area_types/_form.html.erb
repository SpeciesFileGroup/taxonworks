<%= form_for(@geographic_area_type) do |f| %>
    <%= render 'shared/errors', object: @geographic_area_type %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
