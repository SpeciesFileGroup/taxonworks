A password reset has been requested for your TaxonWorks account (<%= @user.email %>).

If you did not request (or no longer want) a password reset for you account please ignore this message. Otherwise please follow the link below to set a new password. 

<%= password_reset_url(token: @token) %>