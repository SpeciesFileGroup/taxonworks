<%= content_for :see do -%>

    <ul class="no_bullets">
      <!-- new content should be in <li> -->
      <%= content_tag(:li, tasks_serials_serial_similar_link(@serial)) %>
    </ul>

<% end %>

<%= render(partial: 'shared/shared_data/user_defined/show', locals: {object: @serial}) -%>

