<%= form_with(model: anatomical_part) do |form| %>
  <% if anatomical_part.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(anatomical_part.errors.count, "error") %> prohibited this anatomical_part from being saved:</h2>

      <ul>
        <% anatomical_part.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_area :name %>
  </div>

  <div>
    <%= form.label :uri, style: "display: block" %>
    <%= form.text_area :uri %>
  </div>

  <div>
    <%= form.label :uri_label, style: "display: block" %>
    <%= form.text_area :uri_label %>
  </div>

  <div>
    <%= form.label :is_material, style: "display: block" %>
    <%= form.check_box :is_material %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
