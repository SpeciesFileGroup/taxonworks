<ul>
  <% @recent_objects.each do |r| -%>
    <li> <%= link_to(object_tag(r) + " on " + object_tag(r.taxon_name), r.taxon_name.metamorphosize) -%> <%= object_time_since_update_tag(r) -%> 
        ago
      </li>
  <% end %>
</ul>
<%= content_tag(:em, "No recently updated or created taxon name classifications.") if @recent_objects.empty? -%>

