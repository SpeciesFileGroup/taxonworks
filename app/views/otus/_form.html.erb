<%= form_for(@otu) do |f| %>
  <% if @otu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@otu.errors.count, "error") %> prohibited this otu from being saved:</h2>

      <ul>
      <% @otu.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
