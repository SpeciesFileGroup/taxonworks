<%= content_for :tasks do -%>
  <%= content_tag(:li, link_to('Transcribe', sqed_depiction_breakdown_task_path(@depiction.sqed_depiction), 'data-no-turbolink' => 'true')) if @depiction.sqed_depiction -%>
<% end %>

<%= content_for :associated do -%><!---- some reason this isn't rendering? ---->
  <% if @depiction.sqed_depiction -%>
    <h3> Sqed Depiction </h3>
    <p>
    <strong>Metadata map:</strong>
    <%= @depiction.sqed_depiction.metadata_map %>
    </p>
  <% end %>
<% end %>

<%= render(partial: 'shared/data/project/show', locals: {object: @depiction}) -%>


